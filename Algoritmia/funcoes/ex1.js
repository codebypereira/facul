function getMin(value1, value2) {
    if (value1 < value2) 
        return value1;

    else
    return value2;

    //OU return (value1 < value2) ? 
}

console.log(getMin(10, 5));
console.log(getMin(3, 7));
console.log(getMin(-1, -5));